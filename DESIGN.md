# DESIGN.md — UI/UXデザイン仕様

## デザインコンセプト

Anthropic「Claude」のデザイン言語を模倣。温かみのあるオフホワイト＋テラコッタアクセントで、クリーン・ミニマル・高い余白感を実現。

## カラーシステム

| トークン | 値 | 用途 |
|---------|-----|------|
| `cream-50` | `#FDFAF6` | メインエリア背景 |
| `cream-100` | `#F9F4EC` | サイドバー背景 |
| `cream-200` | `#F2EAD9` | ボーダー・ディバイダー |
| `cream-300` | `#E8DCCB` | ホバー・スクロールバー |
| `clay-100` | `#FAEAE2` | ユーザーメッセージ背景 |
| `terracotta-500` | `#D97757` | プライマリアクセント（ブランドカラー） |
| `terracotta-600` | `#C4623F` | ホバー状態 |
| `stone-800` | `#3A3633` | プライマリテキスト |
| `stone-500` | `#857F79` | セカンダリテキスト |

## レイアウト

```
┌──────────────┬──────────────────────────────────┐
│  サイドバー   │           ヘッダー               │
│  (256px)     ├──────────────────────────────────┤
│              │                                  │
│  会話リスト   │       メッセージエリア             │
│              │       (max-w-3xl 中央揃え)        │
│              │                                  │
│              ├──────────────────────────────────┤
│              │       入力エリア                  │
└──────────────┴──────────────────────────────────┘
```

- サイドバー: 256px（展開）/ 56px（折りたたみ）
- コンテンツ最大幅: `max-w-3xl`（768px）
- 全体高さ: `h-screen overflow-hidden`

## コンポーネント仕様

### Sidebar
- 折りたたみボタンで256px⇔56pxをアニメーション遷移
- アクティブ会話: テラコッタ背景 + 白文字
- ホバー時に削除ボタン表示（絶対配置）
- フッターに "Powered by DeepSeek" クレジット

### MessageBubble
- ユーザー: 右寄せ、clay背景、Userアバター
- アシスタント: 左寄せ、Botアバター（ストリーミング中はアニメーション）
- コードブロック: ダーク背景(`#1e1e2e`)、コピーボタン（ホバーで表示）
- ストリーミングカーソル: `cursor-blink` アニメーション
- メッセージ全体コピー: ホバーで表示

### ChatInput
- `react-textarea-autosize` (1〜12行)
- Enter送信 / Shift+Enter改行
- ストリーミング中は「停止」ボタンに切替（Squareアイコン）
- 40,000文字超でアンバー警告スタイル

### ModelSelector
- ドロップダウン（外クリックで閉じる）
- Flash: Zapアイコン（アンバー）
- Pro: Brainアイコン（テラコッタ）
- 選択中モデルにドット表示 + ハイライト

## アクセシビリティ

- すべてのボタンに `title` 属性（スクリーンリーダー対応）
- 入力フォーカスに `ring-2` スタイル
- セマンティックHTML: `<aside>`, `<main>`, `<header>`, `<nav>`, `<ul>/<li>`
- 色だけに依存しない情報伝達（アイコン併用）

## アニメーション

| クラス | 定義 | 用途 |
|--------|------|------|
| `animate-[cursor-blink_1s_step-end_infinite]` | 1秒点滅 | ストリーミングカーソル |
| `animate-fade-in` | 0.15s フェードイン | メッセージ表示 |
| `animate-pulse` | Tailwindデフォルト | ストリーミング中のアバター |
